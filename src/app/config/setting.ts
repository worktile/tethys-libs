import { ThySiteSettings } from '@tethys/pro/core';

export const DEFAULT_GLOBAL_SETTING: ThySiteSettings = {
    theme: 'light',
    primaryColor: '#6698ff',
    layout: 'side',
    showHeader: true,
    showFooter: true,
    fixSiderbar: true,
    fixedHeader: true,
    splitMenu: false
};
